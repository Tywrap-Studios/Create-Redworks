{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "",
  "authors": [
    "AuthorExample"
  ],
  "contact": {
  },
  "license": "CC0",
  "icon": "assets/${id}/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      {
        "adapter": "kotlin",
        "value": "com.example.template.platforms.fabric.TemplateFabric"
      }
    ]
  },
  "mixins": [
    "template-common.mixins.json",
    "template-fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15",
    "fabric-language-kotlin": "*",
    "minecraft": "${minecraft}",
    "create": "${create}"
  }
}